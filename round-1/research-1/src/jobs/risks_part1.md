
---

## Risks this research surfaced for the experiment

Each item names the design change it implies. These are ordered by how expensive they become if discovered after the run rather than before it.

### R1 — Anchor projection is a MANDATORY baseline, not related work. *(the plan's stated contingency (a), resolved)*
The plan asked what to do "if Kim & Han need no target-side compute." The text says they **do** need it — §3.4: *"We only forward the fixed anchor pool Z through m_u and construct its anchor matrix Â_{m_u}"* — so the strongest overlap is refuted. **But the baseline is still mandatory**, because the decisive question is whether the two methods occupy the same *operating point*, and App. D.1 settles that they do: 75 forward passes buy 0.832/0.952 [1].
**Design change.** Add a fifth baseline. Implement both cheap forms (training-free signed cosine; source-trained logistic probe over ACS), `k=5` anchor pool, **uniform `f=5/8` layer rule** (App. C, leak-free — never the per-model `BEST_LAYER` dictionary, which is selected using the held-out target's own axis). Include a Gemma-like family so the documented failure (Gemma ten-way 0.16) is inside the comparison. Disclose that their per-prompt scores had to be aggregated to a model-level statistic, which the original does not do. **Cost of discovering this after the run: the primary comparison is unfixable without a re-run.**

### R2 — The slope claim must ride on the continuous axis. *(the plan's stated contingency (b), resolved)*
No published ratio-scaled severity rating of harmful *prompts* exists (three query families searched; labelled absence of evidence). Worse, the spacing assumption is not merely unverified but **contradicted**: perceived severity *"consistently followed an exponential growth as the harmful content became more severe"* [16]. HarmLevelBench's own Table 7 confirms the rungs are four ordered *categorical* facets with no metric anchor, and rungs 6→7 move one facet while 3→4 moves three [19].
**Design change.** (i) The Hill slope is reported **only** on the continuous anchored-interpolation axis, with units stated as *per unit of anchor-interpolation distance, not per unit of severity*. (ii) On the ordinal ladder, report ED50 as a **rung index with a rank-based interval**, and if a per-rung slope is shown at all, add a **monotone re-spacing robustness check** (re-fit under an exponential rung spacing per [16]) and claim only that the slope *ordering* across models is preserved. (iii) Pre-register both, so the ordinal slope is not silently promoted if the continuous axis disappoints.

### R3 — Template sensitivity is perfectly confounded with the grouping variable unless the readout rule is pre-registered per family. *(the plan's stated contingency (d))*
The primary endpoint groups by family; chat templates also group by family. Concretely, the global rule (index −1 after `apply_chat_template(..., add_generation_prompt=True, tokenize=True)`) is **wrong for three of thirteen families, for three different reasons**: Qwen3 and SmolLM3 emit `<think>` as the first token by default; MiniCPM's template does not read `add_generation_prompt` at all, making the flag a **silent no-op** [23][24][25].
**Design changes, all pre-registered before any model is loaded.** (i) Fix the global rule and enumerate the three exceptions in the paper's methods section, not its appendix. (ii) `enable_thinking=False` for Qwen3 *and* SmolLM3, with the note that the resulting suffixes differ — `</think>\n\n` vs `</think>\n` — so they cannot be assumed identical. (iii) Use the `tokenize=True` call path for **Llama-3.2, Gemma-2, Gemma-3, InternLM2.5**, all of which emit `bos_token` inside the jinja *and* have `add_bos_token=True`, so a `tokenize=False` → `tokenizer(text)` round trip **doubles the BOS**. (iv) Normalise the leading-space token variant for **Phi-4-mini** (`<|assistant|>`) and **H2O-Danube3** (`<|answer|>`), the only two families whose suffix does not end in `\n`. (v) Never prepend `bos_token_id` manually — for SmolLM2 the BOS string *is* `<|im_start|>`, the family's own turn marker. (vi) Exclude system-role messages for **Gemma-2** (the template raises) and pre-register that **Gemma-3** silently folds system content into the first user turn.

### R4 — Four families must be dropped, and the roster is size-mismatched to the population.
`StableLM-2` self-reports weak refusal — *"It is also willing to output potentially harmful outputs or misinformation when the user requests it"* — so nearly all its cells right-censor [25]. `MiniCPM`, `InternLM2.5`, `H2O-Danube3` have no usable abliterated triad partner at the right size/generation, and no quantified refusal evidence.
Separately: the census's modal abliteration target is **3–8B (41%)** while a CPU-only budget forces **≤2B** [15], and a sibling artifact in this run recorded only **7 usable abliterated triads** across 13 families.
**Design change.** Drop the four. State plainly that the roster is **family-matched but size-mismatched** to the population and that **triad scarcity, not family coverage, is the binding constraint** — do not claim a representative sample. Include at least one **Heretic**-produced checkpoint (54% of Q1-2026 production) since Hurtado reports his activation gap *misses* Heretic recipes [2][15].

### R5 — Cluster at the family level, and report a transfer MATRIX, not a fold average.
Two independent results make this non-optional. 2605.05427 finds *"refusal and compliance tendencies are stable within model families across generations and scales"* [17] — so family members are not independent observations and treating them as such inflates apparent transfer. 2607.06596 measures the own-family advantage directly at **+0.172 (95% CI [+0.158, +0.185])** on a leak-free basis and prescribes *"cross-family transfer matrices: a single-pairing accuracy overstates the safety a monitor delivers against a lineage it was not calibrated on"* [6].
**Design change.** (i) Cluster bootstrap at the **family** level, never the checkpoint level. (ii) Report a full **family × family transfer matrix**, not an average over held-out folds. (iii) Adopt 2607.06596's three-way decomposition (family obviousness / instrument capability / residual interaction) and its **rotation control pair** — target-directed rotation should recover detection where a matched random rotation should not. All three are cheap to add now and will be asked for if absent.

### R6 — The behavioural ground truth needs two judges from different families.
2605.05427: *"over-refusal measurements are highly stable across evaluators while harmful compliance judgments vary substantially, motivating the use of heterogeneous evaluator ensembles"* [17]. WOAH 2025: LLM judges show *"low to moderate agreement with humans"* on harmfulness ranking [18]. StrongREJECT's own evidence shows substring matching is **negatively** correlated with human judgment (Spearman −0.394, bias 0.484 ± 0.03) [11].
**Design change.** Two judges from different model families on the compliance side; report their disagreement rate as a first-class number; never use a regex scorer for behavioural ground truth. Use the verified formula `(1 − refusal) × (convincingness + specificity − 2) / 8`.

### R7 — The mechanistic leg is a validation, not a discovery, and one of its sources is a live threat.
Three 2026 papers plus Hurtado's own appendix already establish the encode/write dissociation [22][10][26][2]. Writing it as new would be refuted on sight.
**Design change.** Frame the mechanistic leg explicitly as *continuous, dose-parameterised validation of an already-established dissociation* — locating **where along a graded axis** the two channels separate and whether that crossing point is the transferable quantity. Separately, **disclose 2606.08044 as a scope limit**: it constructs, by design, a model that defeats *"a strong fixed probe on clean activations"*, and our screen is exactly that. We screen the wild population, not an adaptive adversary — the same limit Hurtado states for his own audit.

### R8 — Two premises of the design are contradicted in the literature and must be cited against.
(i) **Refusal may not be one linear direction.** 2502.17420 finds *"multiple independent directions and even multi-dimensional concept cones"* [ICML 2025], contradicting 2406.11717's single-direction claim; and 2501.08145 finds *"refusal mechanisms exhibit nonlinear, multidimensional characteristics that vary by model architecture and layer"* across six models in three families [7]. (ii) **What refusal reads differs by family.** 2609.14759: *"Llama reads broad moral content; Qwen reads beyond the single harm cue but is unresolved at our sample size; GPT-OSS reads harm"* [10].
**Design change.** Cite both as contradicting evidence in the paper rather than omitting them, and pre-register the consequence: if the anchored (ED50, slope) transfers *despite* (i) and (ii), that is a stronger result than if the premises had held — so state the prediction in advance and let the family-level result adjudicate it. Do not quietly assume a single linear family-invariant axis.

### R9 — The plan's own numbers contain three high-severity errors. Fix them before drafting.
`0.83/0.95` excludes Gemma and is a four-family figure (Gemma: 0.16, near chance); `+0.46%` is `Δ = +0.46` absolute across an α sweep, beaten by the target's own native direction at +0.55; Hurtado's evaluation is n=94, not 273; and the StrongREJECT formula omits a `−2`. Full ledger in `## Citation ledger`.
**Design change.** Treat the `citation_defects` list as a pre-draft checklist. Every one of these would have been caught by a reviewer.
