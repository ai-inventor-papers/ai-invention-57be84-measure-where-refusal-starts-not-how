# Superseded — do NOT read these as results

These 14 per-checkpoint records come from an earlier Phase D pass that used a buggy
prompt-prefix construction. They were kept only as provenance for the fix, are
referenced by no code, and their numbers do **not** appear anywhere in
`method_out.json`.

The authoritative per-checkpoint records are in `results/ckpt/`.
